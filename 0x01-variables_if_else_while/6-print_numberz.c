#include <stdio.h>
#include <stdlib.h>
/**
 *main -code to display single digits in base10
 *
 * Return:(0) Always
 */
int main(void)
{
	int q;

	for (q = 48; q < 58; q++)
	{
		putchar(q);
	}
	putchar('\n');

	return (0);
}
