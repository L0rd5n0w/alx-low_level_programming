#include <stdio.h>
#include <stdlib.h>
/**
 *main -code to display single digits in base10
 *
 * Return:(0) Always
 */
int main(void)
{
	int q;

	for (q = 0; q <= 9; q++)
	{
		printf("%d", q);
	}
	printf("\n");

	return (0);
}
